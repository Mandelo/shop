server:
  port: 7000 # 端口

spring:
  application:
    name: config-server # 应用名
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Mandelo/shop
          search-paths: shop-config
          username: mandelo
          password: luoxiao121391